# Medical Imaging Dependencies for DuetMind Adaptive
# Core medical imaging libraries

# Medical image formats and I/O
nibabel>=5.0.0           # NIfTI, ANALYZE, and other neuroimaging formats
pydicom>=2.4.0          # DICOM file format support
pydicom-data>=1.1.0     # Sample DICOM data for testing
simpleitk>=2.3.0        # ITK-based medical image processing

# Neuroimaging analysis
nilearn>=0.10.0          # Machine learning for neuroimaging
nipype>=1.8.0           # Neuroimaging workflow engine
niworkflows>=1.7.0      # Common workflows for neuroimaging

# BIDS (Brain Imaging Data Structure) support
bids-validator>=1.13.0   # BIDS dataset validation
pybids>=0.15.0          # BIDS dataset management and querying

# Radiomics and feature extraction
pyradiomics>=3.1.0      # Radiomics feature extraction
radiomics>=3.0.1        # Additional radiomics tools

# Image registration and preprocessing
antspyx>=0.4.0          # ANTs registration in Python
dipy>=1.7.0             # Diffusion imaging processing

# Quality control and visualization
mriqc>=23.1.0           # MRI quality control metrics
dmriprep>=0.5.0         # dMRI preprocessing QC

# DICOM networking and server tools
pynetdicom>=2.0.0       # DICOM networking protocol
dicomweb-client>=0.59.0 # DICOMweb client for PACS integration

# Image processing utilities
scikit-image>=0.21.0    # General image processing
opencv-python>=4.8.0    # Computer vision utilities
pillow>=10.0.0          # Python Imaging Library

# Medical data standards
fhir.resources>=7.0.0   # FHIR R4+ support for medical data
hl7apy>=1.3.4          # HL7 message processing

# Anonymization and privacy
pydicom-data>=1.1.0    # DICOM anonymization tools
cryptography>=41.0.0   # Additional encryption for medical data

# Performance and parallel processing
joblib>=1.3.0          # Parallel processing for large datasets
dask>=2023.9.0         # Distributed computing for imaging pipelines
numba>=0.58.0          # JIT compilation for numerical computation

# Database and storage
sqlalchemy>=2.0.0      # Database ORM
psycopg2-binary>=2.9.0 # PostgreSQL adapter
boto3>=1.28.0          # AWS S3 for cloud storage