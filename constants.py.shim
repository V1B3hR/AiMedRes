"""
Compatibility shim for constants module.
This module has moved to aimedres.core.constants

This shim will be removed in version 2.0.0
"""
import warnings
warnings.warn(
    "Importing from 'constants' at root level is deprecated. "
    "Use 'from aimedres.core.constants import ...' instead. "
    "This compatibility shim will be removed in version 2.0.0",
    DeprecationWarning,
    stacklevel=2
)

# Re-export everything from new location
from aimedres.core.constants import *
